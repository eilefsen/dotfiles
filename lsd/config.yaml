classic: false
blocks:
  - permission
  - user
  - group
  - size
  - name
color:
  when: auto
  theme: default
icons:
  when: auto
  theme: fancy
  separator: " "
layout: grid
size: short
total-size: false
permission: rwx
sorting:
  # Possible values: extension, name, time, size, version
  column: name
  reverse: false
  # Possible values: first, last, none
  dir-grouping: first
symlink-arrow: ⇒
